### 问题分析
当前训练反馈和训练计划消息在对话框中一直沉底，而不是按发送时间排序，可能是由于：
1. 这些自动发送的消息的sendTime字段格式或值有问题
2. 排序逻辑可能存在特殊处理，导致这些消息被排除在正常排序之外

### 解决方案
1. **增强消息排序逻辑**：修改`fetchMessages`函数中的排序逻辑，确保：
   - 所有消息类型（包括训练计划和反馈）都按发送时间排序
   - 处理可能的sendTime字段缺失或格式问题
   - 确保排序逻辑对所有消息类型都适用

2. **修改位置**：`c:\Users\17254\Desktop\软件 (2)\frontend\src\views\ChatDialog.vue`

3. **具体修改**：
   - 优化`fetchMessages`函数中的排序代码
   - 确保所有消息都正确包含在排序逻辑中
   - 处理sendTime字段的边界情况

### 预期效果
修改后，训练计划和训练反馈消息将与普通消息一样，根据发送时间在对话中正确排序，不再一直沉底。